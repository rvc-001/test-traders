# ########################
# # Node.js gitignore
# ########################

# --- Dependencies ---
# Never commit node_modules. This directory contains all your project's
# dependencies, which can be re-installed using 'npm install' or 'yarn install'.
/node_modules

# --- Logs ---
# Log files are often environment-specific and can grow very large.
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
pnpm-debug.log*

# --- Environment Variables ---
# **CRITICAL**: Never commit your environment variables.
# This file contains secrets like API keys, database passwords, etc.
# Create a .env.example file to show what variables are needed.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*

# --- Build & Distribution ---
# These folders contain the compiled or bundled output of your project
# (e.g., from TypeScript, Webpack, Babel).
/dist
/build
/public/build
/.out
/.next
/.nuxt

# --- Caches ---
# Various tools create cache files to speed up performance.
.npm
.eslintcache
.prettiercache
.parcel-cache
.yarn/cache
.yarn/unplugged
.pnpm-store

# --- Test Output ---
# Reports and coverage generated from running tests.
/coverage
/reports
.nyc_output

# --- IDE & Editor Config ---
# User-specific settings for editors.
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- OS Files ---
# System-specific files generated by macOS, Windows, and Linux.
.DS_Store
Thumbs.db
desktop.ini

# --- Optional ---
# If you are using pm2
/pm2_logs
*.pm2.json